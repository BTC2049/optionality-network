# 福利雷達一次性設定

完成後，系統每天台灣時間約上午 9:10 輪替進入交易所活動中心、項目任務平台與空投網站，整理最多 20 筆新福利，並在每天凌晨自動整理已截止活動。

## 1. 建立資料表

1. 打開 Supabase `SQL Editor`
2. 建立新查詢
3. 貼上 `benefit-radar.sql` 全部內容
4. 按 `Run`

## 2. 建立抓取函式

1. 到 Supabase `Edge Functions`
2. 建立函式，名稱填 `benefit-fetcher`
3. 將 `benefit-fetcher.ts` 全部貼進函式
4. 關閉 `Verify JWT`
5. 部署函式

## 3. 設定抓取密碼

1. 到 Edge Functions 的 `Secrets`
2. 新增：
   - 名稱：`BENEFIT_FETCH_SECRET`
   - 值：自行設定一串至少 24 字元的隨機文字
3. 不要把這串密碼上傳到 GitHub

## 4. 測試一次

在 Edge Function 測試工具送出：

- Method：`POST`
- Header：`x-benefit-secret`
- Value：剛設定的密碼
- Body：`{}`

成功後會顯示新增筆數。福利會出現在網站的「福利探索」。

## 5. 每日排程

1. 打開 `benefit-fetcher-cron.sql`
2. 把 `YOUR_BENEFIT_FETCH_SECRET` 換成同一串抓取密碼
3. 到 Supabase `SQL Editor` 建立新查詢
4. 貼上全部內容並按 `Run`

## 注意

- 系統最多新增 20 筆，不會為了湊數重複舊福利。
- 不再把新聞搜尋結果當成福利；只接受來源網站本身的活動頁。
- 清理時採用直接來源白名單：不屬於 `benefit_sources` 的舊自動資料會撤下，因此不需要逐家補新聞黑名單。
- 來源分成交易所、Quest／項目任務、空投平台三組，每天每組輪替抽取。
- 初始來源存放在 `benefit_sources` 資料表，可直接新增、停用或替換，不必修改 Edge Function。
- 每個新發現來源每天最多 3 筆、同類福利最多 6 筆，避免單一品牌洗版。
- 多元性限制只影響每天抓取與網站排序，不會因單一來源累積較多就撤下已驗證福利。
- 華語內容優先；英文內容只作補充，網站標題與摘要會先整理成中文。
- 系統會排除短網址、Telegram、Discord、表單與常見可疑錢包領取網址。
- 「加密產業詞」與「實際福利詞」必須同時成立；資訊安全加密、政府觀光及一般活動不會收錄。
- 純教學、平台比較、推薦文章、評測與 SEO 內容不屬於可索取福利，也會自動排除。
- 來源頁沒有新活動時，當天可能少於 10 筆。
- 系統會優先辨識官方頁面寫出的截止日期；例如活動到 6 月 9 日，6 月 10 日會移到「過期活動」。
- 找不到明確期限時，資料預設保留 30 天後離開主要列表。
- 各來源的活動供給每天不同，因此新增數量可能少於 10 筆；寧可少，也不自動補入新聞、低品質或重複內容。

## 清理先前的英文或單一來源資料

若網站已經有大量 OKX 或英文活動：

1. 在 SQL Editor 建立新查詢
2. 貼上 `rebalance-benefits.sql`
3. 按 `Run`
4. 回到 Edge Function 手動測試 `benefit-fetcher` 一次
